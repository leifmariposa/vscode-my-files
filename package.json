{
    "name": "special-open-file",
    "displayName": "special-open-file",
    "description": "",
    "version": "0.0.1",
    "publisher": "leifmariposa",
    "engines": {
        "vscode": "^1.16.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [
        "onCommand:extension.sayHello"
    ],
    "main": "./out/src/extension",
    "contributes": {
        "commands": [{
            "command": "extension.sayHello",
            "title": "Hello World"
        }],
        "configuration": {
            "type": "object",
            "title": "Special Open File configuration",
            "properties": {
                "specialOpenFile.folders": {
                    "type": "string",
                    "description": "The paths to search when the specialOpenFile command is run, separated by pipes",
                    "default": ""
                }
            }
        }
    },
    "scripts": {
        "vscode:prepublish": "tsc -p ./",
        "compile": "tsc -watch -p ./",
        "postinstall": "node ./node_modules/vscode/bin/install",
        "test": "node ./node_modules/vscode/bin/test"
    },
    "devDependencies": {
        "typescript": "^2.0.3",
        "vscode": "^1.0.0",
        "mocha": "^2.3.3",
        "@types/node": "^6.0.40",
        "@types/mocha": "^2.2.32"
    },
    "dependencies": {
        "promise":"^8.0.1"
    }
}